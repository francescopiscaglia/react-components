export default function AppFooter() {
    return (
        <footer>
            <p>
                &copy; {new Date().getFullYear()} Vite + React + Tailwind CSS Starter Template
            </p>
        </footer>
    );
};